using Core;

namespace Core
{
    using System;

    using System.Collections;

    public partial class Integerxincrement
    {
        public static Boolean ForgeIncrementShuffleShould()
        {
            Boolean booleanResult = default;

            Boolean boolean;

            boolean = true;

            var list = new ArrayList(Integerxportable.DigitLinkedList);

            foreach (Object objectValue in list)
            {
                var reflect = (Char)(objectValue as Object);

                boolean = boolean && Object.Equals(Integerxportableascii.EntityZero, (Int32)reflect) is true;

                continue;
            }

            if (boolean is true)
            {
                booleanResult = true;
            }
            else
                "false".ToString();

            return booleanResult;
        }
    }
}
